@echo off
echo Setting up authentication tables...
echo.

node -e "const db = require('./config/database'); (async () => { try { const connection = await db.getPool().getConnection(); console.log('Creating organizations table...'); await connection.execute(`CREATE TABLE IF NOT EXISTS organizations ( id VARCHAR(36) PRIMARY KEY, org_name VARCHAR(255) NOT NULL, admin_first_name VARCHAR(100) NOT NULL, admin_last_name VARCHAR(100) NOT NULL, email VARCHAR(255) UNIQUE NOT NULL, phone VARCHAR(50), address TEXT, password_hash VARCHAR(255) NOT NULL, api_key VARCHAR(64) UNIQUE, status ENUM('active', 'inactive', 'suspended') DEFAULT 'active', created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP, INDEX idx_email (email), INDEX idx_api_key (api_key), INDEX idx_status (status) )`); console.log('Creating users table...'); await connection.execute(`CREATE TABLE IF NOT EXISTS users ( id VARCHAR(36) PRIMARY KEY, organization_id VARCHAR(36), first_name VARCHAR(100) NOT NULL, last_name VARCHAR(100) NOT NULL, email VARCHAR(255) UNIQUE NOT NULL, phone VARCHAR(50), password_hash VARCHAR(255) NOT NULL, role ENUM('individual', 'admin', 'member') DEFAULT 'individual', status ENUM('active', 'inactive') DEFAULT 'active', created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP, FOREIGN KEY (organization_id) REFERENCES organizations(id) ON DELETE SET NULL, INDEX idx_email (email), INDEX idx_organization_id (organization_id), INDEX idx_role (role) )`); console.log('Checking if columns exist in stix_reports...'); const [columns] = await connection.execute(`SHOW COLUMNS FROM stix_reports LIKE 'organization_id'`); if (columns.length === 0) { console.log('Adding organization_id column...'); await connection.execute(`ALTER TABLE stix_reports ADD COLUMN organization_id VARCHAR(36) NULL`); } const [userColumns] = await connection.execute(`SHOW COLUMNS FROM stix_reports LIKE 'user_id'`); if (userColumns.length === 0) { console.log('Adding user_id column...'); await connection.execute(`ALTER TABLE stix_reports ADD COLUMN user_id VARCHAR(36) NULL`); } console.log('Adding indexes...'); await connection.execute(`CREATE INDEX idx_organization_id ON stix_reports(organization_id)`).catch(() => {}); await connection.execute(`CREATE INDEX idx_user_id ON stix_reports(user_id)`).catch(() => {}); console.log('Creating system organization for legacy reports...'); await connection.execute(`INSERT IGNORE INTO organizations (id, org_name, admin_first_name, admin_last_name, email, password_hash, status) VALUES ('system-legacy', 'System', 'System', 'Admin', 'system@threadchain.local', '$2b$10$dummyhash', 'active')`); console.log('Updating legacy reports...'); const [result] = await connection.execute(`UPDATE stix_reports SET organization_id = 'system-legacy' WHERE organization_id IS NULL`); console.log(`Updated ${result.affectedRows} legacy reports`); connection.release(); console.log(''); console.log('✅ Authentication tables created successfully!'); console.log('✅ stix_reports table updated with organization tracking'); console.log('✅ Legacy reports assigned to System organization'); process.exit(0); } catch (error) { console.error('❌ Error:', error.message); process.exit(1); } })();"

echo.
echo Done!
pause
